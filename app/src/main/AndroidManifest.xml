<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mufinds">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MUFINDS">
        <activity android:name=".PrincipalActivity"></activity>
        <activity android:name=".ConfiguracionActivity" />
        <activity android:name=".IniciActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".RegistroActivity2" />
        <activity android:name=".RecuperarContrasenaActivity" />
        <activity android:name=".RecuperarUsuarioActivity" />
    </application>

</manifest>